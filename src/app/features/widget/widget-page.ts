import {Type} from "@angular/core";
import {Widget} from "./widget";
/*
export interface WidgetPage {
  id: number;
  label: string;
  title: string;
  columns: number;
  widgets: Widget[];
  userId: number;
}
*/
